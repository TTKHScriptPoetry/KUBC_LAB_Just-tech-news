<div class="comments">
   {{!-- {{#each post.comments}}
   <section class="comment">
      <div class="meta">
         {{user.username}} on {{created_at}}
      </div>
      <div class="text">
         {{comment_text}}
      </div>
   </section>
   {{/each}} --}}

   {{!-- Because arrays don't have named properties like objects do, we're forced to access the array as this. --}}
   {{#each this}}
   <section class="comment">
      <div class="meta">
         {{!-- {{user.username}} on {{created_at}} --}}

         {{!-- // how to call method defined in helpers format_date(created_at) --}}
         {{user.username}} on {{format_date created_at}} 
      </div>
      <div class="text">
         {{comment_text}}
      </div>
   </section>
   {{/each}}
</div>